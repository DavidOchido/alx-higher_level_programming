#!/usr/bin/node
/*Update this script to replace the value 12 with 89  */
#!/usr/bin/node

const myObject = {
  type: 'object',
  value: 12
};

console.log(myObject);

myObject.value = 89; // Updating the value from 12 to 89

console.log(myObject);

